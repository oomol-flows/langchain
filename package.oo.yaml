icon: ./icon.png
version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    set -e
    pip install --upgrade pip
    pip install -r requirements.txt
dependencies:
  oomol-preview: 0.0.1
name: langchain
description: LangChain is a framework for developing applications powered by
  large language models (LLMs).
displayName: LangChain
releaseNotes: release the first version
